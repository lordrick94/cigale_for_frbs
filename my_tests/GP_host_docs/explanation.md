# Adding Background object removal capability for PYPEIT
This document will provide prompts to guide Claude development of the of a scripts to add a background object capabilities when reducing spectra with pypeit.

## Claude

Be sure to review the CLAUDE.md file [/home/lordrick/Projects/CLAUDE.md] for general guidelines.

## Context
Review the following before making any changes:

  - The pypeit code: `/home/lordrick/Projects/F4_Repos/PypeIt`
  - Pypeit docs: https://pypeit.readthedocs.io/en/stable/index.html
  - Host GP repo:`/home/lordrick/Projects/fun_side_projects/PYPEIT_SUMMER_WORK/HostSub_GP`


## TODOs
1. Read this repo and all it's code and all the context. 
    - Make a doc guiding me on any Science and or statistic or machine learning concepts I should review before embarking on this project 
    - summarise what this does with diagrams and or animations and make a folder explaining all that
    - Make planning docs for how we could implement this for pypeit spectra


## Coding

Follow these guidelines:

- Code in Python
- Use inline comments to explain the code.
- Place imports at the top of modules
- Keep code specific to automation in the automation/ directory.
- Reuse any code in this repository to help you
- Include a description of inputs/outputs in the doc string of all methods 
- Limit lines of code to 80 characters per line
- Add "Generated by Lordrick and Claude" in the doc string of all methods you create


## Always
 - Ask questions when something is not clear before running scripts
 - Report all you progress each step of the way in the Logs section of this doc


## Logs

### 2026-08-26 — TODO item 1 complete

- Read the full HostSub_GP codebase (`src/hostsub_gp/`, all modules,
  scripts, docs) and the relevant parts of PypeIt
  (`spec2dobj`, `slittrace`, `specobj`/`specobjs`, `find_objects.py`,
  `extraction.py`, `core/skysub.py`, `core/extract.py`,
  `core/spatialprofile.py`, `pypeit_steps.py`, `exposure.py`) to
  understand both the GP-based host-subtraction algorithm and PypeIt's
  own object-finding/sky-subtraction/extraction pipeline.
- Wrote `onboarding/01_concepts_to_review.md`: a primer on the
  science/stats/ML concepts used in this repo (Gaussian Process
  regression, kernel design incl. the custom non-stationary
  `EmissionLineKernel`, GP mean functions as informative priors, robust
  statistics/MAD, PSF/seeing matching, emission-line + cross-correlation
  redshift detection, synthetic photometry, astrometric registration,
  and the JAX idioms needed to read the code), ordered by how central
  each concept is to the codebase.
- Wrote `onboarding/pipeline_overview/README.md`: an illustrated summary
  of the full pipeline (Mermaid diagrams for end-to-end data flow, the
  three-GP host-model composition, the module dependency graph, and the
  slit's mask/host/sky spatial-region layout), plus a table of what
  changes in the PypeIt output files.
- Wrote `onboarding/pypeit_integration_planning/` (4 docs): current
  state of the existing post-hoc PypeIt integration, two integration
  options (harden the post-hoc workflow vs. an in-line PypeIt
  `Extract`-stage integration, with concrete PypeIt file:line anchors for
  where the latter would hook in), a risks/open-questions doc, and a
  phased roadmap.
- Found and documented two real bugs while reading the code: a kernel-type
  string typo (`"HostProfie"`) in `host_model.py` that breaks the
  single-filter photometric-prior path, and a PypeIt API drift
  (`pypeit.core.extract.fit_profile` should be
  `pypeit.core.spatialprofile.fit_profile`) that breaks the
  `extraction="optimal"` path against the currently checked-out PypeIt
  version. Also flagged several docs/code mismatches (stale
  spectrograph list, empty parameters.rst stub) — all detailed in
  `onboarding/pypeit_integration_planning/03_risks_open_questions.md`.
- Did not make any code changes yet — this pass was read-only research
  and planning docs, per the TODO item's scope. No code fixes applied
  (including the two bugs found) pending your go-ahead, since "Ask
  questions when something is not clear before running scripts" /
  changing code applies here too.